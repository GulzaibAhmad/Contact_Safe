import React from 'react';
import { createRoot } from 'react-dom/client'; // Import from the correct source
import App from './App';

const root = createRoot(document.getElementById('root')); // Use createRoot from the correct source
root.render(<App />);
